function setup() {
    createCanvas(500, 500)
    colorMode(HSB, 360, 100, 100, 100);
    rectMode(CENTER);
    noStroke();
}

function draw() {
    background(1);
    fill(207,109,10)
    circle(250,250, 50)
}